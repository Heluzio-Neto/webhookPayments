// BillingType Enum
export enum BillingType {
    BOLETO = 'BOLETO',
    CREDIT_CARD = 'CREDIT_CARD',
    PIX = 'PIX',
  }
  
  // PaymentStatus Enum
  export enum PaymentStatus {
    CONFIRMED = 'CONFIRMED',
    RECEIVED = 'RECEIVED',
    OVERDUE = 'OVERDUE',
    PENDING = 'PENDING',
  }